import { describe, it, expect } from "vitest";


describe('EpisodeDetailsUtils.getEpisodeDetails', () => {
    it('calls fetch with correct URL and returns data', async () => {
        const response = {id: 2, number: 2, name: 'Test Episode'};
    })
})
